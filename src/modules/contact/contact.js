import { createElement } from "../../utility.js";
import "./contact.css"

export function Contact(content) {
    const contactPage = createElement({ classes: ["contact-page"], parent: content });
}